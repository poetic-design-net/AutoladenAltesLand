---
import { client, queries } from '../lib/sanity.ts';

// Kontaktdaten aus Sanity holen
let kontaktData;
try {
  kontaktData = await client.fetch(queries.kontakt);
} catch (error) {
  console.error('Error fetching kontakt data:', error);
}

// Fallback-Daten wenn Sanity nicht verfügbar
if (!kontaktData) {
  kontaktData = {
    whatsappNummer: "491621234567",
    alexBild: "/alex.jpg",
    alexBildAlt: "Alexander Phillipowski"
  };
}

// WhatsApp-Nummer formatieren (Entferne alle Nicht-Ziffern außer +)
const phoneNumber = kontaktData.whatsappNummer?.replace(/[^0-9+]/g, '').replace('+', '') || "491621234567";

// Dynamic online status based on German business hours
const now = new Date();
const germanTime = new Date(now.toLocaleString("en-US", {timeZone: "Europe/Berlin"}));
const hours = germanTime.getHours();
const day = germanTime.getDay(); // 0 = Sunday, 6 = Saturday

// Business hours: Mon-Fri 9-17, Sat 9-14
const isBusinessHours = 
  (day >= 1 && day <= 5 && hours >= 9 && hours < 17) || // Mon-Fri
  (day === 6 && hours >= 9 && hours < 14); // Saturday

// Different messages based on time
const defaultMessage = isBusinessHours 
  ? "Hallo Alex! Ich interessiere mich für ein neues Fahrzeug."
  : "Hallo Alex! Ich interessiere mich für ein neues Fahrzeug. Gerne können wir morgen sprechen.";

// Status text based on time
const statusText = isBusinessHours ? "Online" : "Antwortet bald";
const statusColor = isBusinessHours ? "green" : "yellow";
const statusDotAnimation = isBusinessHours ? "animate-pulse" : "";
---

<!-- WhatsApp Floating Button -->
<div id="whatsapp-widget" class="fixed bottom-6 right-6 z-50">
  <!-- Main Button -->
  <a
    href={`https://wa.me/${phoneNumber}?text=${encodeURIComponent(defaultMessage)}`}
    target="_blank"
    rel="noopener noreferrer"
    class="whatsapp-button group flex items-center justify-center w-14 h-14 bg-green-500 hover:bg-green-600 rounded-full shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-110"
    aria-label="Chat on WhatsApp"
  >
    <svg class="w-7 h-7 text-white" fill="currentColor" viewBox="0 0 24 24">
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.149-.67.149-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414-.074-.123-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
    </svg>
    
    <!-- Hover Card -->
    <div class="absolute right-16 bottom-0 opacity-0 scale-95 group-hover:opacity-100 group-hover:scale-100 transition-all duration-300 pointer-events-none origin-bottom-right">
      <div class="bg-white rounded-lg shadow-xl p-3 w-52 flex items-center gap-3">
        <img 
          src={kontaktData.alexBild || "/alex.jpg"} 
          alt={kontaktData.alexBildAlt || "Alexander Phillipowski - WhatsApp Kontakt"}
          class="w-10 h-10 rounded-full object-cover ring-1 ring-gray-100"
          loading="lazy"
          decoding="async"
        />
        <div class="flex-1">
          <p class="font-semibold text-gray-900 text-sm">Alex</p>
          <p class={`text-xs flex items-center gap-1 ${statusColor === 'green' ? 'text-green-500' : 'text-yellow-600'}`}>
            <span class={`w-1.5 h-1.5 rounded-full flex-shrink-0 ${statusColor === 'green' ? 'bg-green-500 animate-pulse' : 'bg-yellow-500'}`}></span>
            {statusText}
          </p>
        </div>
      </div>
      <!-- Arrow pointing to button -->
      <div class="absolute -right-2 bottom-3 w-0 h-0 border-t-8 border-t-transparent border-b-8 border-b-transparent border-l-8 border-l-white"></div>
    </div>
  </a>
  
  <!-- Pulse Animation -->
  <div class="absolute inset-0 rounded-full bg-green-500 animate-ping opacity-25 pointer-events-none"></div>
  
  <!-- Welcome Message (shows on first visit) -->
  <div id="whatsapp-welcome" class="absolute bottom-16 right-0 bg-white rounded-lg shadow-xl p-4 w-72 opacity-0 scale-95 pointer-events-none transition-all duration-300">
    <button id="close-welcome" class="absolute top-2 right-2 text-gray-400 hover:text-gray-600">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>
    <div class="flex items-center gap-3 mb-3">
      <img 
        src={kontaktData.alexBild || "/alex.jpg"} 
        alt={kontaktData.alexBildAlt || "Alexander Phillipowski - Direkter WhatsApp Kontakt"}
        class="w-10 h-10 rounded-full object-cover"
        loading="lazy"
        decoding="async"
      />
      <div>
        <p class="font-semibold text-gray-900">Alex</p>
        <p class={`text-xs flex items-center gap-1 ${statusColor === 'green' ? 'text-green-500' : 'text-yellow-600'}`}>
          <span class={`w-2 h-2 rounded-full ${statusColor === 'green' ? 'bg-green-500 animate-pulse' : 'bg-yellow-500'}`}></span>
          {statusText}
        </p>
      </div>
    </div>
    <p class="text-sm text-gray-600 mb-3">
      {isBusinessHours 
        ? "Hallo! 👋 Ich bin gerade online. Schreib mir gerne direkt!"
        : "Hallo! 👋 Ich bin gerade nicht im Büro, antworte dir aber sobald wie möglich!"}
    </p>
    <a
      href={`https://wa.me/${phoneNumber}?text=${encodeURIComponent(defaultMessage)}`}
      target="_blank"
      rel="noopener noreferrer"
      class="block w-full bg-green-500 hover:bg-green-600 text-white text-center py-2 rounded-lg text-sm font-medium transition-colors"
    >
      Jetzt chatten
    </a>
  </div>
</div>

<style>
  /* Smooth entrance animation */
  @keyframes slideInUp {
    from {
      transform: translateY(100px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  
  #whatsapp-widget {
    animation: slideInUp 0.5s ease-out 1s both;
  }
  
  /* Hide on very small screens if needed */
  @media (max-width: 360px) {
    #whatsapp-widget {
      bottom: 1rem;
      right: 1rem;
    }
    
    .whatsapp-button {
      width: 3rem;
      height: 3rem;
    }
    
    .whatsapp-button svg {
      width: 1.5rem;
      height: 1.5rem;
    }
  }
</style>

<script>
  // Show welcome message after 3 seconds (only on first visit)
  const hasSeenWelcome = localStorage.getItem('whatsappWelcomeShown');
  const welcomeMessage = document.getElementById('whatsapp-welcome');
  const closeButton = document.getElementById('close-welcome');
  
  if (!hasSeenWelcome && welcomeMessage) {
    setTimeout(() => {
      welcomeMessage.classList.remove('opacity-0', 'scale-95', 'pointer-events-none');
      welcomeMessage.classList.add('opacity-100', 'scale-100');
      localStorage.setItem('whatsappWelcomeShown', 'true');
      
      // Auto-hide after 10 seconds
      setTimeout(() => {
        hideWelcome();
      }, 10000);
    }, 3000);
  }
  
  function hideWelcome() {
    if (welcomeMessage) {
      welcomeMessage.classList.add('opacity-0', 'scale-95', 'pointer-events-none');
      welcomeMessage.classList.remove('opacity-100', 'scale-100');
    }
  }
  
  // Close button functionality
  closeButton?.addEventListener('click', hideWelcome);
</script>