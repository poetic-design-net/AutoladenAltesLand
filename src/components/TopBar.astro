---
// Get current German time for online status
const now = new Date();
const germanTime = new Date(now.toLocaleString("en-US", {timeZone: "Europe/Berlin"}));
const hours = germanTime.getHours();
const day = germanTime.getDay();

// Check if Alex is online
const isOnline = 
  (day >= 1 && day <= 5 && hours >= 9 && hours < 17) || 
  (day === 6 && hours >= 9 && hours < 14);

// Format current day for display
const dayNames = ['So', 'Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa'];
const currentDay = dayNames[day];

// Get next opening time
const getNextOpening = () => {
  if (isOnline) return null;
  
  // If it's Saturday afternoon or Sunday, next opening is Monday
  if (day === 0 || (day === 6 && hours >= 14)) {
    return "Mo 9:00 Uhr";
  }
  // If it's after hours on weekday
  if (day >= 1 && day <= 5 && hours >= 17) {
    // If Friday evening, next is Saturday
    if (day === 5) return "Sa 9:00 Uhr";
    // Otherwise next day
    return `${dayNames[day + 1]} 9:00 Uhr`;
  }
  // If it's early morning
  if (hours < 9) {
    return "Heute 9:00 Uhr";
  }
  return "Mo 9:00 Uhr";
};

const nextOpening = getNextOpening();
---

<!-- Ultra-thin Top Bar -->
<div class="fixed top-0 left-0 right-0 bg-white/95 backdrop-blur-sm border-b border-gray-100 h-10" style="z-index: 55;">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-full">
    <div class="flex items-center justify-between h-full text-xs">
      
      <!-- Left: Status with hover for hours -->
      <div class="flex items-center gap-6">
        <!-- Current Status with hover -->
        <div class="group relative flex items-center gap-1.5">
          <span class={`w-2 h-2 rounded-full ${isOnline ? 'bg-green-500 animate-pulse' : 'bg-yellow-500'}`}></span>
          <span class={`font-medium ${isOnline ? 'text-green-600' : 'text-gray-500'}`}>
            {isOnline ? `Jetzt erreichbar` : `Wieder ab ${nextOpening}`}
          </span>
          
          <!-- Hover tooltip with business hours -->
          <div class="absolute top-full left-0 mt-2 opacity-0 scale-95 group-hover:opacity-100 group-hover:scale-100 transition-all duration-200 pointer-events-none">
            <div class="bg-gray-900 text-white text-xs px-3 py-2 rounded-lg whitespace-nowrap">
              <div class="font-medium mb-1">Öffnungszeiten</div>
              <div>Mo-Fr: 9:00 - 17:00 Uhr</div>
              <div>Sa: 9:00 - 14:00 Uhr</div>
              <div>So: Geschlossen</div>
              <!-- Arrow -->
              <div class="absolute -top-1 left-4 w-2 h-2 bg-gray-900 transform rotate-45"></div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Right: Contact Icons with hover info -->
      <div class="flex items-center gap-3">
        <!-- Phone with hover -->
        <div class="group relative">
          <a 
            href="tel:+491621234567" 
            class="flex items-center text-gray-600 hover:text-brand-green transition-colors"
          >
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
          </a>
          <!-- Hover tooltip -->
          <div class="absolute top-full left-1/2 -translate-x-1/2 mt-2 opacity-0 scale-95 group-hover:opacity-100 group-hover:scale-100 transition-all duration-200 pointer-events-none">
            <div class="bg-gray-900 text-white text-xs px-2 py-1 rounded whitespace-nowrap">
              0162 123 4567
              <div class="absolute -top-1 left-1/2 -translate-x-1/2 w-2 h-2 bg-gray-900 transform rotate-45"></div>
            </div>
          </div>
        </div>
        
        <!-- Email with hover -->
        <div class="group relative">
          <a 
            href="mailto:info@autoladen-altesland.de" 
            class="flex items-center text-gray-600 hover:text-brand-green transition-colors"
          >
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </a>
          <!-- Hover tooltip -->
          <div class="absolute top-full left-1/2 -translate-x-1/2 mt-2 opacity-0 scale-95 group-hover:opacity-100 group-hover:scale-100 transition-all duration-200 pointer-events-none">
            <div class="bg-gray-900 text-white text-xs px-2 py-1 rounded whitespace-nowrap">
              info@autoladen-altesland.de
              <div class="absolute -top-1 left-1/2 -translate-x-1/2 w-2 h-2 bg-gray-900 transform rotate-45"></div>
            </div>
          </div>
        </div>
        
        <!-- WhatsApp with hover -->
        <div class="group relative">
          <a 
            href="https://wa.me/491621234567"
            target="_blank"
            class="flex items-center text-gray-600 hover:text-green-500 transition-colors"
          >
            <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.149-.67.149-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414-.074-.123-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
            </svg>
          </a>
          <!-- Hover tooltip -->
          <div class="absolute top-full left-1/2 -translate-x-1/2 mt-2 opacity-0 scale-95 group-hover:opacity-100 group-hover:scale-100 transition-all duration-200 pointer-events-none">
            <div class="bg-gray-900 text-white text-xs px-2 py-1 rounded whitespace-nowrap">
              WhatsApp
              <div class="absolute -top-1 left-1/2 -translate-x-1/2 w-2 h-2 bg-gray-900 transform rotate-45"></div>
            </div>
          </div>
        </div>
      </div>
      
    </div>
  </div>
</div>

<style>
  /* Smooth fade-in animation */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  div:first-of-type {
    animation: fadeIn 0.3s ease-out;
  }
</style>